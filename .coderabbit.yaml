# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
reviews:
  path_filters:
    # Installed upstream skill packages are agent tooling, not application code.
    - "!.agents/**"
